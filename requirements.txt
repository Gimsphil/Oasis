# 전기 견적,수량산출 시스템 의존성
# 설치 명령: pip install -r requirements.txt

# GUI 프레임워크
PyQt6>=6.4.0

# 데이터 처리
pandas>=1.5.0
numpy>=1.23.0

# 데이터베이스
# sqlite3는 Python 표준 라이브러리에 포함됨

# 엑셀 파일 처리
openpyxl>=3.0.10
xlrd>=2.0.1

# 기타 유틸리티
python-dateutil>=2.8.0
pytz>=2022.1

# CAD 연동 (Phase 6)
ezdxf>=0.18.0  # DXF/DWG 파일 파싱

# OCR (이미지 텍스트 인식)
pytesseract>=0.3.10          # OCR 엔진
pdf2image>=1.16.3            # PDF → 이미지 변환
Pillow>=9.5.0                # 이미지 처리
opencv-python>=4.7.0         # 이미지 전처리 (선택)
